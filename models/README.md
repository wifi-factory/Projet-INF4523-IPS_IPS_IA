# Models

This directory is reserved for local copies of the trained model and metadata.

By default, the backend loads the artifacts from the external paths observed in
the provided metadata, unless `IPS_MODEL_PATH` and `IPS_METADATA_PATH` override
them.
