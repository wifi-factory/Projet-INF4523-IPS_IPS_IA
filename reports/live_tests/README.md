# Validation live

Ce dossier conserve de petites sorties JSON de validation du pipeline live.

Les fichiers JSON versionnes servent de traces de verification ponctuelle
(capture, prediction, alertes, blocage simule). Les logs `.log` eventuellement
presents ici sont des sorties locales de debug et restent ignores par Git.
